# Effigent BYO run storage — CloudFormation.
# Prefer the pre-filled copy from the dashboard (Storage → "Download the
# CloudFormation template"), which bakes in your workspace's ExternalId and
# Effigent's account id. This raw copy requires both as parameters:
#
#   aws cloudformation deploy --stack-name effigent-storage \
#     --template-file effigent-byo-storage.yaml \
#     --capabilities CAPABILITY_NAMED_IAM \
#     --parameter-overrides EffigentAccountId=<12 digits> ExternalId=<eff-ext-…>
#
# Keep in sync with packages/dashboard/src/lib/byo-template.ts (the served copy).
AWSTemplateFormatVersion: '2010-09-09'
Description: >-
  Effigent BYO run storage: a private, encrypted S3 bucket in YOUR account plus
  a least-privilege cross-account role (PutObject/GetObject on this one bucket
  only). Delete the stack (or the role) to revoke Effigent's access instantly.

Parameters:
  EffigentAccountId:
    Type: String
    Description: Effigent's AWS account id (the only principal allowed to assume the role).
    AllowedPattern: '^[0-9]{12}$'
  ExternalId:
    Type: String
    Description: Per-workspace external id (generated by Effigent) required on every AssumeRole.
    MinLength: 8
  BucketName:
    Type: String
    Default: ''
    Description: Optional bucket name. Leave empty to let AWS generate one.
  KmsKeyArn:
    Type: String
    Default: ''
    Description: Optional CMK ARN. Leave empty for SSE-S3 (AES256).

Conditions:
  HasBucketName: !Not [!Equals [!Ref BucketName, '']]
  HasKms: !Not [!Equals [!Ref KmsKeyArn, '']]

Resources:
  RunBucket:
    Type: AWS::S3::Bucket
    Properties:
      BucketName: !If [HasBucketName, !Ref BucketName, !Ref 'AWS::NoValue']
      PublicAccessBlockConfiguration:
        BlockPublicAcls: true
        IgnorePublicAcls: true
        BlockPublicPolicy: true
        RestrictPublicBuckets: true
      BucketEncryption:
        ServerSideEncryptionConfiguration:
          - ServerSideEncryptionByDefault: !If
              - HasKms
              - SSEAlgorithm: 'aws:kms'
                KMSMasterKeyID: !Ref KmsKeyArn
              - SSEAlgorithm: AES256
            BucketKeyEnabled: true

  EffigentAccessRole:
    Type: AWS::IAM::Role
    Properties:
      RoleName: !Sub 'effigent-run-storage-${AWS::StackName}'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Principal:
              AWS: !Sub 'arn:aws:iam::${EffigentAccountId}:root'
            Action: 'sts:AssumeRole'
            Condition:
              StringEquals:
                'sts:ExternalId': !Ref ExternalId
      Policies:
        - PolicyName: effigent-run-blobs
          PolicyDocument:
            Version: '2012-10-17'
            Statement:
              - Sid: EffigentRunBlobs
                Effect: Allow
                Action: ['s3:PutObject', 's3:GetObject']
                Resource: !Sub '${RunBucket.Arn}/*'
              - !If
                - HasKms
                - Sid: EffigentRunBlobsKms
                  Effect: Allow
                  Action: ['kms:GenerateDataKey', 'kms:Decrypt']
                  Resource: !Ref KmsKeyArn
                - !Ref 'AWS::NoValue'

Outputs:
  Bucket:
    Description: Enter this as "Bucket" in the Effigent Storage settings.
    Value: !Ref RunBucket
  Region:
    Description: Enter this as "Region".
    Value: !Ref 'AWS::Region'
  RoleArn:
    Description: Enter this as "Role ARN".
    Value: !GetAtt EffigentAccessRole.Arn
  KmsKeyArn:
    Description: Enter this as "KMS key ARN" (empty = SSE-S3).
    Value: !Ref KmsKeyArn
