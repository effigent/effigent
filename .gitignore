node_modules/
packages/*/dist/
packages/*/.next/
packages/*/out/
data/
*.log
.env.local
.env*.local
effigent-report.html
effigent-report.json
demo-report.html
demo-report.json
.env

# single-file distribution: everything in /dist is generated, but the bundled
# CLI is committed so the GitHub Action and raw-URL install work
/dist/*
!/dist/effigent.cjs

# internal business docs stay local
effigent-mvp-spec.md
.vercel
.env*

# TypeScript incremental build info
*.tsbuildinfo

# research datasets are private session content — never commit
research/**/*.json
